{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd SlidingWindow/ && g++ -g UdpSocket.cpp Timer.cpp udp.cpp hw2.cpp -o hw2",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}